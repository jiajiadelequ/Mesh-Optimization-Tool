# OBJ Batch Optimizer

Run `run_mesh_opt_tool.bat` to open the desktop tool.

Main files:

- `mesh_opt_tool.py`: Tkinter desktop app
- `run_mesh_opt_tool.bat`: launcher

The tool reads source OBJ files in read-only mode and writes optimized files only to the output directory you choose.
